---
"prisma-pglite-bridge": patch
---

Internal consistency: align `Pglite` identifiers with the canonical
`PGlite` casing across test helpers, benchmark instrumentation, and
local variables. No public API changes.
